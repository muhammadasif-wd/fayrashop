import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Fayrashop",
  description:
    "Fayrashop is a dynamic multivendor e-commerce platform that connects customers with a wide range of products from trusted sellers. Offering a seamless shopping experience, Fayrashop is dedicated to providing quality, variety, and convenience in one place. Whether you're looking for fashion, electronics, home essentials, or more, Fayrashop ensures a secure and enjoyable shopping journey for all.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
